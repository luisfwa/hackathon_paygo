var subtotal = 0;

window.onload = function()	{
	document.setInterval(callback, 200);
}

function callback()	{
	$.post("http://www.conwebdesign.com.br/PayGo/get_carrinho", function(result)	{
		
	})
}